import {useEffect,useRef} from "react";
import {evidenceDemo} from "../data/content";
type Props={open:boolean;onClose:()=>void;triggerRef:React.RefObject<HTMLElement|null>};
export function EvidenceDialog({open,onClose,triggerRef}:Props){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!open)return;const key=(e:KeyboardEvent)=>{if(e.key==="Escape")onClose()};document.addEventListener("keydown",key);ref.current?.querySelector<HTMLElement>("button")?.focus();return()=>{document.removeEventListener("keydown",key);triggerRef.current?.focus()}},[open,onClose,triggerRef]);if(!open)return null;return <div className="dialog-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div ref={ref} className="dialog" role="dialog" aria-modal="true" aria-labelledby="evidence-title"><button className="dialog-close" onClick={onClose} aria-label="Close evidence example">×</button><p className="status">{evidenceDemo.status}</p><h2 id="evidence-title">{evidenceDemo.title}</h2><p>{evidenceDemo.intro}</p>{evidenceDemo.rows.map(r=><dl key={r.key} className="evidence-row"><dt>{r.key}</dt><dd>{r.value}</dd></dl>)}<h3>{evidenceDemo.limitationsTitle}</h3><ul>{evidenceDemo.limitations.map(x=><li key={x}>{x}</li>)}</ul><button className="btn btn-secondary" onClick={onClose}>Close</button></div></div>}
