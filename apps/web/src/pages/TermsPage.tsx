type Props={onNavigate:(p:string)=>void};export function TermsPage({onNavigate}:Props){return <main id="main" className="section"><div className="shell reading"><p className="eyebrow">Prototype terms</p><h1>Prototype terms notice</h1><p className="lead">This is a frontend prototype. No account is created, no relationship report is calculated and no service contract is formed through this interface. Production terms remain subject to review.</p><button className="btn btn-secondary" onClick={()=>onNavigate("/#/")}>Back to Bazodiac Relationships</button></div></main>}
