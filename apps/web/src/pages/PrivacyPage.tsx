type Props={onNavigate:(p:string)=>void};export function PrivacyPage({onNavigate}:Props){return <main id="main" className="section"><div className="shell reading"><p className="eyebrow">Privacy</p><h1>Privacy principles</h1><p className="lead">Production must minimise personal data, require explicit purpose and consent, keep provider secrets server-side, and define retention, withdrawal and deletion before launch.</p><p>This prototype creates no account, session or relationship report.</p><button className="btn btn-secondary" onClick={()=>onNavigate("/#/")}>Back to Bazodiac Relationships</button></div></main>}
