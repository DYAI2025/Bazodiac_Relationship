import{FormEvent,useState}from"react";import{authAdapter,validateLogin}from"../data/auth";type Props={onNavigate:(p:string)=>void;next:string};export function LoginPage({onNavigate}:Props){const[errors,setErrors]=useState<Record<string,string>>({});const[status,setStatus]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const x={email:String(f.get("email")||""),password:String(f.get("password")||"")};const v=validateLogin(x);setErrors(v);if(Object.keys(v).length)return;setStatus("Logging in…");await authAdapter.login(x);setStatus("Frontend prototype complete. No session was created and no data was sent.");(e.currentTarget.elements.namedItem("password")as HTMLInputElement).value=""}return <main id="main" className="section auth"><div className="form-card"><p className="eyebrow">Prototype only</p><h1>Welcome back</h1><p>Log in to continue your Bazodiac Relationship work.</p><form onSubmit={submit} noValidate><label>Email address<input name="email" type="email" autoComplete="email"/></label>{errors.email&&<p className="error">{errors.email}</p>}<label>Password<input name="password" type="password" autoComplete="current-password"/></label>{errors.password&&<p className="error">{errors.password}</p>}<button className="btn btn-primary">Log in</button><p role="status">{status}</p></form><p>New to Bazodiac? <button className="link" onClick={()=>onNavigate("/#/register")}>Create an account</button></p></div></main>}
