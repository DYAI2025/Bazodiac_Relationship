import{useState}from"react";import{opportunityShadowContent as c}from"../data/content";export function OpportunityShadow(){const[mode,setMode]=useState<"shadow"|"opportunity">("opportunity");const x=mode==="shadow"?c.shadow:c.opportunity;return <section className="section paper"><div className="shell"><p className="eyebrow">{c.eyebrow}</p><h2>{c.headline}</h2><p className="lead">{c.body}</p><div className="split-controls" role="tablist">{c.controls.map(v=><button key={v.id} role="tab" aria-selected={mode===v.id} onClick={()=>setMode(v.id as typeof mode)}>{v.title}</button>)}</div><article className={`split-panel ${mode}`} role="tabpanel"><h3>{x.title}</h3><p>{x.body}</p></article><p>{c.qualification}</p></div></section>}
