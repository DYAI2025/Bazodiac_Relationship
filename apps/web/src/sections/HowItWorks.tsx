import{howItWorksContent as c}from"../data/content";type Props={onNavigate:(p:string)=>void};export function HowItWorks({onNavigate}:Props){return <section id={c.id} className="section paper"><div className="shell"><p className="eyebrow">{c.eyebrow}</p><h2>{c.headline}</h2><div className="steps">{c.steps.map(s=><article key={s.label}><span>{s.label}</span><h3>{s.title}</h3><p>{s.body}</p></article>)}</div><p className="boundary">{c.boundary}</p><button className="btn btn-secondary" onClick={()=>onNavigate("/#/inside-report")}>{c.cta}</button></div></section>}
