import{useState}from"react";import{reportContent as c}from"../data/content";export function Report(){const[tab,setTab]=useState("shared");const[open,setOpen]=useState(false);const x=tab==="person-a"?c.personA:tab==="person-b"?c.personB:c.shared;return <section id={c.id} className="section"><div className="shell"><p className="eyebrow">{c.eyebrow}</p><h2>{c.headline}</h2><span className="status">{c.status}</span><div className="tabs" role="tablist">{c.tabs.map(t=><button key={t.id} role="tab" aria-selected={tab===t.id} tabIndex={tab===t.id?0:-1} onClick={()=>setTab(t.id)}>{t.label}</button>)}</div><article className="report-card" role="tabpanel"><h3>{x.title}</h3><p>{x.body}</p>{"classification" in x&&<b>{x.classification}</b>}<button className="btn btn-secondary" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?c.conditions.hide:c.conditions.show}</button>{open&&<div className="conditions"><div><h4>Intensifying conditions</h4><ul>{c.conditions.intensifying.map(v=><li key={v}>{v}</li>)}</ul></div><div><h4>Moderating conditions</h4><ul>{c.conditions.moderating.map(v=><li key={v}>{v}</li>)}</ul></div><p><strong>Counter-hypothesis:</strong> {c.conditions.counter}</p></div>}</article></div></section>}
