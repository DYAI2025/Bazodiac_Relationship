import {heroContent} from "../data/content";import{NetworkSVG}from"../components/NetworkSVG";type Props={onNavigate:(p:string)=>void};export function Hero({onNavigate}:Props){return <section className="hero"><NetworkSVG/><div className="shell hero-copy"><p className="eyebrow">{heroContent.eyebrow}</p><div className="brand-hero">BAZODIAC<small>Relationships</small></div><h1>{heroContent.headlineStart} <em>{heroContent.headlineEm}</em></h1><p className="lead">{heroContent.intro}</p><div className="actions"><button className="btn btn-primary" onClick={()=>onNavigate("/#/register?next=create-report")}>{heroContent.primary}</button><a className="btn btn-secondary" href="/#/how-it-works" onClick={e=>{e.preventDefault();onNavigate("/#/how-it-works")}}>{heroContent.secondary}</a></div><p>Already have an account? <button className="link" onClick={()=>onNavigate("/#/login?next=create-report")}>Log in.</button></p></div></section>}
