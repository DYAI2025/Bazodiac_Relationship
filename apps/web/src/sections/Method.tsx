import{useRef,useState}from"react";import{methodContent as c}from"../data/content";import{EvidenceDialog}from"../components/EvidenceDialog";type Props={onNavigate:(p:string)=>void};export function Method({onNavigate}:Props){const[open,setOpen]=useState(false);const ref=useRef<HTMLButtonElement>(null);return <section id={c.id} className="section paper"><div className="shell"><p className="eyebrow">{c.eyebrow}</p><h2>{c.headline}</h2><p className="lead">{c.body}</p><div className="steps">{c.principles.map(p=><article key={p.title}><h3>{p.title}</h3><p>{p.body}</p></article>)}</div><div className="actions"><button className="btn btn-secondary" onClick={()=>onNavigate("/#/method")}>{c.readMethod}</button><button ref={ref} className="btn btn-primary" onClick={()=>setOpen(true)}>{c.showEvidence}</button></div></div><EvidenceDialog open={open} onClose={()=>setOpen(false)} triggerRef={ref}/></section>}
